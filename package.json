{
  "name": "slop-scorer",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Score any artifact 0-100 for generated/template character, with citable evidence.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "tsc -b --clean",
    "verify": "npm run typecheck && npm run build && npm test && npm run backtest",
    "backtest": "npm run build --silent && node scripts/backtest.mjs",
    "build:gauntlet-pool": "npm run build --silent && node scripts/build-gauntlet-pool.mjs",
    "measure:list-rules": "npx tsc -b packages/mcp-server --force && node scripts/measure-list-rules.mjs",
    "capture:code-corpus": "npm run build --silent && node scripts/capture-code-corpus.mjs",
    "capture:web-corpus": "npm run build --silent && node scripts/capture-web-corpus.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "playwright": "^1.62.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
