node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
coverage/
.DS_Store

.corpus-cache/

.vercel
.env*

# Audit and verification captures. Hundreds of full-page PNGs, regenerated on every run and
# never referenced by the site. The 2026-08-24 UI audit left this decision open; this is it.
.audit-shots/

# A stray next build at the repo root (rather than in apps/web) leaves one of these behind.
.next/

# Local audio corpora built by scripts/capture-audio-corpus.mjs. Measurements of somebody's
# recording, kept out of the repository on purpose: see packages/detectors-audio/README.md.
.corpus-cache/audio/
